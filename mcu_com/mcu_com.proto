syntax = "proto3";

message SearchRequest {
  string query = 1;
  int32 page_number = 2;
  int32 result_per_page = 3;
  string test = 4;
}

message ZoneData {
  repeated uint32 lph = 1 [packed=false];
  repeated string names = 2;
  uint32 zone_count = 3;
}
